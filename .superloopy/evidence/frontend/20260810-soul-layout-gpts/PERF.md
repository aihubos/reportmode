# Performance notes

- The prompt is stored as a 21 KB Markdown asset and loaded on demand by the report page.
- No new framework or runtime dependency was added.
- The supplied thumbnail is 689 KB and preserves its original 1672 by 941 dimensions.
- Shared brand CSS and JavaScript remain mirrored in `assets/` and `src/site/assets/`.
