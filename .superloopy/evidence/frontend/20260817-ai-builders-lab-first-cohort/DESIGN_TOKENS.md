# Report Hub token contract

This upload retains the approved AI Builders Lab teaching-page system and adds no new visual language. The governing Report Hub tokens are the shared `DESIGN.md` contract: Toss Blue `#3182F6`, navy ink `#191F28`, white surfaces, 4px spacing scale, Pretendard and Apple-system typography, 18px to 24px radii, and the single subtle floating-menu elevation.

The supplied AI Builders Lab wordmark is used as the archive cover without alteration. The added access panel uses the source page variables (`--blue`, `--line`, `--white`, `--muted`, `--shadow-md`) so it stays within the same system.
